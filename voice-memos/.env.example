# No API key required — the pipeline uses the claude CLI, which authenticates
# via your existing Claude Code session (MAX, PRO, or API plans all work).
#
# Add any other environment variables needed by your setup here.
